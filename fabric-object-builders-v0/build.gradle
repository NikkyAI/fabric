archivesBaseName = "fabric-object-builders"
version = getSubprojectVersion(project, "0.1.1")

dependencies {
    compile project(path: ':fabric-api-base')
    compile project(path: ':fabric-mining-levels-v0')
}
