archivesBaseName = "fabric-item-groups-v0"
version = getSubprojectVersion(project, "0.1.0")

dependencies {
	compile project(path: ':fabric-api-base')
	compile project(path: ':fabric-resource-loader-v0')
}
