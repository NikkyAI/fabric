archivesBaseName = "fabric-renderer-indigo"
version = getSubprojectVersion(project, "0.1.7")

dependencies {
	compile project(path: ':fabric-api-base')
	compile project(path: ':fabric-renderer-api-v1')
}
